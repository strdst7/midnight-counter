# The global ~/.npmrc pins the @midnight-ntwrk scope to https://npm.midnight.network,
# which no longer resolves. These packages are published on the public npm
# registry, so override the scope here (project-local, does not touch global config).
@midnight-ntwrk:registry=https://registry.npmjs.org/
